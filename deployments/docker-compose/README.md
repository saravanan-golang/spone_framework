
copy the configuration file to the configs directory before starting the service.

```
├── configs
│         └── user_service.yml
└── docker-compose.yml
```

running service:

> docker-compose up -d
